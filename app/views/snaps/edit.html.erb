<div class="text-center">
  <%= image_tag @snap.image.url(:medium) %>
</div>
<%= render 'form'  %>
<div class="text-center edit-links">
  <%= link_to "Delete Snap", snap_path(@snap), method: :delete, data: { confirm: "Are you sure you want to delete this post?" } %>
  |
  <%= link_to "cancel", snaps_path %>
</div>
